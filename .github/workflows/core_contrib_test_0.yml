# Do not edit this file.
# This file is generated automatically by executing tox -e generate-workflows

name: Core Contrib Test 0

on:
  workflow_call:
    inputs:
      CORE_REPO_SHA:
        required: true
        type: string
      CONTRIB_REPO_SHA:
        required: true
        type: string

permissions:
  contents: read

env:
  CORE_REPO_SHA: ${{ inputs.CORE_REPO_SHA }}
  CONTRIB_REPO_SHA: ${{ inputs.CONTRIB_REPO_SHA }}
  PIP_EXISTS_ACTION: w

jobs:

  py39-test-instrumentation-openai-v2-oldest:
    name: instrumentation-openai-v2-oldest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-openai-v2-oldest -- -ra

  py39-test-instrumentation-openai-v2-latest:
    name: instrumentation-openai-v2-latest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-openai-v2-latest -- -ra

  py39-test-instrumentation-vertexai-oldest:
    name: instrumentation-vertexai-oldest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-vertexai-oldest -- -ra

  py39-test-instrumentation-vertexai-latest:
    name: instrumentation-vertexai-latest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-vertexai-latest -- -ra

  py39-test-instrumentation-google-genai-oldest:
    name: instrumentation-google-genai-oldest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-google-genai-oldest -- -ra

  py39-test-instrumentation-google-genai-latest:
    name: instrumentation-google-genai-latest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-google-genai-latest -- -ra

  py39-test-resource-detector-containerid:
    name: resource-detector-containerid 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-resource-detector-containerid -- -ra

  py39-test-resource-detector-azure-0:
    name: resource-detector-azure-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-resource-detector-azure-0 -- -ra

  py39-test-resource-detector-azure-1:
    name: resource-detector-azure-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-resource-detector-azure-1 -- -ra

  py39-test-sdk-extension-aws-0:
    name: sdk-extension-aws-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-sdk-extension-aws-0 -- -ra

  py39-test-sdk-extension-aws-1:
    name: sdk-extension-aws-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-sdk-extension-aws-1 -- -ra

  py39-test-distro:
    name: distro 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-distro -- -ra

  py39-test-opentelemetry-instrumentation:
    name: opentelemetry-instrumentation 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-opentelemetry-instrumentation -- -ra

  py39-test-instrumentation-aiohttp-client:
    name: instrumentation-aiohttp-client 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aiohttp-client -- -ra

  py39-test-instrumentation-aiohttp-server:
    name: instrumentation-aiohttp-server 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aiohttp-server -- -ra

  py39-test-instrumentation-aiopg:
    name: instrumentation-aiopg 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aiopg -- -ra

  py39-test-instrumentation-arangodb-oldest:
    name: instrumentation-arangodb-oldest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-arangodb-oldest -- -ra

  py39-test-instrumentation-arangodb-latest:
    name: instrumentation-arangodb-latest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-arangodb-latest -- -ra

  py39-test-instrumentation-aws-lambda:
    name: instrumentation-aws-lambda 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aws-lambda -- -ra

  py39-test-instrumentation-botocore-0:
    name: instrumentation-botocore-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-botocore-0 -- -ra

  py39-test-instrumentation-botocore-1:
    name: instrumentation-botocore-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-botocore-1 -- -ra

  py39-test-instrumentation-boto3sqs:
    name: instrumentation-boto3sqs 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-boto3sqs -- -ra

  py39-test-instrumentation-django-0:
    name: instrumentation-django-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-django-0 -- -ra

  py39-test-instrumentation-django-1:
    name: instrumentation-django-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-django-1 -- -ra

  py39-test-instrumentation-django-2:
    name: instrumentation-django-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-django-2 -- -ra

  py39-test-instrumentation-dbapi:
    name: instrumentation-dbapi 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-dbapi -- -ra

  py39-test-instrumentation-boto:
    name: instrumentation-boto 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-boto -- -ra

  py39-test-instrumentation-asyncclick:
    name: instrumentation-asyncclick 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-asyncclick -- -ra

  py39-test-instrumentation-click:
    name: instrumentation-click 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-click -- -ra

  py39-test-instrumentation-elasticsearch-0:
    name: instrumentation-elasticsearch-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-elasticsearch-0 -- -ra

  py39-test-instrumentation-elasticsearch-1:
    name: instrumentation-elasticsearch-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-elasticsearch-1 -- -ra

  py39-test-instrumentation-elasticsearch-2:
    name: instrumentation-elasticsearch-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-elasticsearch-2 -- -ra

  py39-test-instrumentation-falcon-0:
    name: instrumentation-falcon-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-falcon-0 -- -ra

  py39-test-instrumentation-falcon-1:
    name: instrumentation-falcon-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-falcon-1 -- -ra

  py39-test-instrumentation-falcon-2:
    name: instrumentation-falcon-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-falcon-2 -- -ra

  py39-test-instrumentation-falcon-3:
    name: instrumentation-falcon-3 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-falcon-3 -- -ra

  py39-test-instrumentation-fastapi:
    name: instrumentation-fastapi 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-fastapi -- -ra

  py39-test-instrumentation-flask-0:
    name: instrumentation-flask-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-flask-0 -- -ra

  py39-test-instrumentation-flask-1:
    name: instrumentation-flask-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-flask-1 -- -ra

  py39-test-instrumentation-flask-2:
    name: instrumentation-flask-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-flask-2 -- -ra

  py39-test-instrumentation-urllib:
    name: instrumentation-urllib 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-urllib -- -ra

  py39-test-instrumentation-urllib3-0:
    name: instrumentation-urllib3-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-urllib3-0 -- -ra

  py39-test-instrumentation-urllib3-1:
    name: instrumentation-urllib3-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-urllib3-1 -- -ra

  py39-test-instrumentation-requests:
    name: instrumentation-requests 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-requests -- -ra

  py39-test-instrumentation-starlette-oldest:
    name: instrumentation-starlette-oldest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-starlette-oldest -- -ra

  py39-test-instrumentation-starlette-latest:
    name: instrumentation-starlette-latest 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-starlette-latest -- -ra

  py39-test-instrumentation-jinja2:
    name: instrumentation-jinja2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-jinja2 -- -ra

  py39-test-instrumentation-logging:
    name: instrumentation-logging 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-logging -- -ra

  py39-test-exporter-richconsole:
    name: exporter-richconsole 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-exporter-richconsole -- -ra

  py39-test-exporter-prometheus-remote-write:
    name: exporter-prometheus-remote-write 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-exporter-prometheus-remote-write -- -ra

  py39-test-instrumentation-mysql-0:
    name: instrumentation-mysql-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-mysql-0 -- -ra

  py39-test-instrumentation-mysql-1:
    name: instrumentation-mysql-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-mysql-1 -- -ra

  py39-test-instrumentation-mysqlclient:
    name: instrumentation-mysqlclient 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-mysqlclient -- -ra

  py39-test-instrumentation-psycopg2:
    name: instrumentation-psycopg2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-psycopg2 -- -ra

  py39-test-instrumentation-psycopg2-binary:
    name: instrumentation-psycopg2-binary 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-psycopg2-binary -- -ra

  py39-test-instrumentation-psycopg:
    name: instrumentation-psycopg 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-psycopg -- -ra

  py39-test-instrumentation-pymemcache-0:
    name: instrumentation-pymemcache-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymemcache-0 -- -ra

  py39-test-instrumentation-pymemcache-1:
    name: instrumentation-pymemcache-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymemcache-1 -- -ra

  py39-test-instrumentation-pymemcache-2:
    name: instrumentation-pymemcache-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymemcache-2 -- -ra

  py39-test-instrumentation-pymemcache-3:
    name: instrumentation-pymemcache-3 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymemcache-3 -- -ra

  py39-test-instrumentation-pymemcache-4:
    name: instrumentation-pymemcache-4 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymemcache-4 -- -ra

  py39-test-instrumentation-pymongo:
    name: instrumentation-pymongo 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymongo -- -ra

  py39-test-instrumentation-pymysql:
    name: instrumentation-pymysql 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymysql -- -ra

  py39-test-instrumentation-pymssql:
    name: instrumentation-pymssql 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pymssql -- -ra

  py39-test-instrumentation-pyramid:
    name: instrumentation-pyramid 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-pyramid -- -ra

  py39-test-instrumentation-asgi:
    name: instrumentation-asgi 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-asgi -- -ra

  py39-test-instrumentation-asyncpg:
    name: instrumentation-asyncpg 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-asyncpg -- -ra

  py39-test-instrumentation-sqlite3:
    name: instrumentation-sqlite3 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-sqlite3 -- -ra

  py39-test-instrumentation-wsgi:
    name: instrumentation-wsgi 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-wsgi -- -ra

  py39-test-instrumentation-grpc-0:
    name: instrumentation-grpc-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-grpc-0 -- -ra

  py39-test-instrumentation-grpc-1:
    name: instrumentation-grpc-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-grpc-1 -- -ra

  py39-test-instrumentation-sqlalchemy-1:
    name: instrumentation-sqlalchemy-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-sqlalchemy-1 -- -ra

  py39-test-instrumentation-sqlalchemy-2:
    name: instrumentation-sqlalchemy-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-sqlalchemy-2 -- -ra

  py39-test-instrumentation-redis:
    name: instrumentation-redis 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-redis -- -ra

  py39-test-instrumentation-remoulade:
    name: instrumentation-remoulade 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-remoulade -- -ra

  py39-test-instrumentation-celery:
    name: instrumentation-celery 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-celery -- -ra

  py39-test-instrumentation-system-metrics:
    name: instrumentation-system-metrics 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-system-metrics -- -ra

  py39-test-instrumentation-threading:
    name: instrumentation-threading 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-threading -- -ra

  py39-test-instrumentation-tornado:
    name: instrumentation-tornado 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-tornado -- -ra

  py39-test-instrumentation-tortoiseorm:
    name: instrumentation-tortoiseorm 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-tortoiseorm -- -ra

  py39-test-instrumentation-httpx-0:
    name: instrumentation-httpx-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-httpx-0 -- -ra

  py39-test-instrumentation-httpx-1:
    name: instrumentation-httpx-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-httpx-1 -- -ra

  py39-test-util-http:
    name: util-http 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-util-http -- -ra

  py39-test-exporter-credential-provider-gcp:
    name: exporter-credential-provider-gcp 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-exporter-credential-provider-gcp -- -ra

  py39-test-util-genai:
    name: util-genai 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-util-genai -- -ra

  py39-test-propagator-aws-xray-0:
    name: propagator-aws-xray-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-propagator-aws-xray-0 -- -ra

  py39-test-propagator-aws-xray-1:
    name: propagator-aws-xray-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-propagator-aws-xray-1 -- -ra

  py39-test-propagator-ot-trace:
    name: propagator-ot-trace 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-propagator-ot-trace -- -ra

  py39-test-instrumentation-sio-pika-0:
    name: instrumentation-sio-pika-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-sio-pika-0 -- -ra

  py39-test-instrumentation-sio-pika-1:
    name: instrumentation-sio-pika-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-sio-pika-1 -- -ra

  py39-test-instrumentation-aio-pika-0:
    name: instrumentation-aio-pika-0 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aio-pika-0 -- -ra

  py39-test-instrumentation-aio-pika-1:
    name: instrumentation-aio-pika-1 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aio-pika-1 -- -ra

  py39-test-instrumentation-aio-pika-2:
    name: instrumentation-aio-pika-2 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aio-pika-2 -- -ra

  py39-test-instrumentation-aio-pika-3:
    name: instrumentation-aio-pika-3 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aio-pika-3 -- -ra

  py39-test-instrumentation-aiokafka:
    name: instrumentation-aiokafka 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-aiokafka -- -ra

  py39-test-instrumentation-kafka-python:
    name: instrumentation-kafka-python 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-kafka-python -- -ra

  py39-test-instrumentation-kafka-pythonng:
    name: instrumentation-kafka-pythonng 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-kafka-pythonng -- -ra

  py39-test-instrumentation-confluent-kafka:
    name: instrumentation-confluent-kafka 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-confluent-kafka -- -ra

  py39-test-instrumentation-asyncio:
    name: instrumentation-asyncio 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-asyncio -- -ra

  py39-test-instrumentation-cassandra:
    name: instrumentation-cassandra 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-instrumentation-cassandra -- -ra

  py39-test-processor-baggage:
    name: processor-baggage 
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout contrib repo @ SHA - ${{ env.CONTRIB_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python-contrib
          ref: ${{ env.CONTRIB_REPO_SHA }}

      - name: Checkout core repo @ SHA - ${{ env.CORE_REPO_SHA }}
        uses: actions/checkout@v4
        with:
          repository: open-telemetry/opentelemetry-python
          ref: ${{ env.CORE_REPO_SHA }}
          path: opentelemetry-python

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
          architecture: "x64"

      - name: Install tox
        run: pip install tox-uv

      - name: Run tests
        run: tox -e py39-test-processor-baggage -- -ra
